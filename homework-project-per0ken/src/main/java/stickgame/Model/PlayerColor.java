package stickgame.Model;

public enum PlayerColor {
    /**
     * currentPlayerColor gets this value
     * if the next click will paint the line
     * to red.
     */
    RED,
    /**
     * currentPlayerColor gets this value
     * if the next click will paint the line
     * to blue.
     */
    BLUE
}

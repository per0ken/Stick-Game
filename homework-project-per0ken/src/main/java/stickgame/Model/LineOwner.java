package stickgame.Model;

public enum LineOwner {
    /**
     * The ownerMapping gets this value
     * to decide which player owns the line.
     */
    RED,
    /**
     * The ownerMapping gets this value
     * to decide which player owns the line.
     */
    BLUE,
    /**
     * The ownerMapping gets this value
     * if no one owns the square.
     */
    NONE
}
